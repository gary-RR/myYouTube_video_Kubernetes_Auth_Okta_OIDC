#krew **********************************************************************************************************************************

    #*****************************(linux/MAC)************************************************************************
    (
      set -x; cd "$(mktemp -d)" &&
      OS="$(uname | tr '[:upper:]' '[:lower:]')" &&
      ARCH="$(uname -m | sed -e 's/x86_64/amd64/' -e 's/`(arm`)`(64`)`?.*/`1`2/' -e 's/aarch64$/arm64/')" &&
      KREW="krew-${OS}_${ARCH}" &&
      curl -fsSLO "https://github.com/kubernetes-sigs/krew/releases/latest/download/${KREW}.tar.gz" &&
      tar zxvf "${KREW}.tar.gz" &&
      ./"${KREW}" install krew
    )

    export PATH="${KREW_ROOT:-$HOME/.krew}/bin:$PATH"
    #***********************************************************************************************************

    #*****************************(Windows)*********************************************************************
    #Follow "Windows" instructions section here: 
    #https://krew.sigs.k8s.io/docs/user-guide/setup/install/#:~:text=Windows%201%20Make%20sure%20git%20is%20installed.%202,6%20Launch%20a%20new%20command-line%20window.%20More%20items
    #******************************************************************************************************************************************************************************************

#terraform*********************************************************************************************************************************************************************************

    #*************************(linux)***************************************************************************
    sudo snap install terraform --classic
    #***********************************************************************************************************

    #**************************(MAC OS)*************************************************************************
    #1- Add the Terraform repository:
      brew tap hashicorp/tap
    #2- Install Terraform from the new repository by running:
      brew install hashicorp/tap/terraform
    #***************************************************************************************************************************

    #**************************(Windoes)************************************************************************
    #Follow instrations here: https://developer.hashicorp.com/terraform/tutorials/aws-get-started/install-cli#install-terraform
    #***************************************************************************************************************************